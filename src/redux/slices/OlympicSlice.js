import { createSlice } from '@reduxjs/toolkit';

const initialState = {};

const olymPicSlice = createSlice({
  name: 'olympic',
  initialState,
  reducers: {}
});

export const {} = olymPicSlice.actions;
export default olymPicSlice.reducer;
